# demo shop

A tiny fake shop used to show softlint working on real pull requests in this repo.
The rules it's checked against are in [`/softlint.json`](../softlint.json). Open pull requests
touching `demo/` get reviewed by the [softlint workflow](../.github/workflows/softlint.yml).
